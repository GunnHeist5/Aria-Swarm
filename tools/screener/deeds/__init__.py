"""tools/screener/deeds — County Clerk deed pulls. NOT IMPLEMENTED (v1).

TODO: deed history requires browser automation against the Harris County
Clerk's records search (no public REST API). Out of scope for the screener's
v1 by explicit decision in the build brief — no Playwright here. When this
gets built, it belongs in the isolated browser runner, not inline in the
screening pipeline.
"""
