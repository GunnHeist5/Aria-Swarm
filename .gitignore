# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Dirt Screener output (scored workbooks contain lead PII — never commit)
out/
enriched_leads.xlsx

# Browser runner session material — storage_state is a live credential
tools/browser/browser.yaml
storage_state.json

# Local state / secrets (the swarm wallet lives in ~/.automaton, but guard against strays)
*.sqlite
*.sqlite3
wallet.json
.env
.env.*
!.env.example
