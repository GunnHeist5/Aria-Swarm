# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Dirt Screener output (scored workbooks contain lead PII — never commit)
out/
enriched_leads.xlsx

# Browser runner: the session file is a live credential — never commit it.
# (browser.yaml holds only calibrated selectors, not secrets, so it CAN be
#  committed and pulled to the VPS — it is intentionally not ignored.)
storage_state.json
propstream_storage_state.json

# Local state / secrets (the swarm wallet lives in ~/.automaton, but guard against strays)
*.sqlite
*.sqlite3
wallet.json
.env
.env.*
!.env.example
